language: cpp
sudo: required
dist: trusty
compiler:
- gcc
os:
- linux
before_install:
- sudo apt-get -qq update
- sudo apt-get install -y cmake kdevplatform-dev extra-cmake-modules
script:
- mkdir build && cd build && cmake .. && make
